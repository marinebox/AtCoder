#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define rep(i, n) for(int i = 0; i < n; i++)
#define all(v) v.begin(), v.end()

int main(){
    int r;
    cin >> r;

    double ans;
    ans = r * 2 * M_PI;

    // cout << ans << endl;
    cout << fixed << setprecision(10) << ans << endl;
    return 0;
}