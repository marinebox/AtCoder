#include <bits/stdc++.h>
using namespace std;

int main(){
    int h, n;
    cin >> h >> n;
    vector<vector<double>> efficiency(1e4 + 1, vector<double>(2,0))
    // map<int, int>
    //魔力iで出せる最大ダメージを配列化する。
    for(int i = 0; i < n; i++){
        double tmp1, tmp2;
        cin >> tmp1 >> tmp2;
        double eff = tmp1 / tmp2;
        if()
    }
}