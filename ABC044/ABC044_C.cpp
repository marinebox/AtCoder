#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define rep(i, n) for(int i = 0; i < n; i++)
#define all(v) v.begin(), v.end()

int main(){
    int n, a;
    cin >> n >> a;
    vector<int> x(n);
    for(int i = 0; i < n; i++){
        cin >> x[i];
    }

    
}