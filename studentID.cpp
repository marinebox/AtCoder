#include <bits/stdc++.h>
using namespace std;

// bool void(string s){

//     return false;
// }

int main(){
    int lim = 2000;
    char course = 'B';
    string id;
    int year = 16;

    for(int i = 0; i < lim; i++){
        char *number;
        sprintf(number, "%05d", i);
        // cout << number << endl;
        printf("%s\n", number);
    }

    // cout << id << endl;
    return 0;
}