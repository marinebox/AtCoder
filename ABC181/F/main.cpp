#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;
    vector<pair<int, int>> kugi(n);
    for(int i = 0; i < n; i++){
        int x, y;
        cin >> x >> y;
        kugi[i] = make_pair(x, y);
    }

    
}