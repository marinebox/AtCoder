#include <bits/stdc++.h>
using namespace std;



int main(){
    int n;
    cin >> n;
    vector<int>a(n);
    for (int tsumugi = 0; tsumugi < n; tsumugi++){
        cin >> a[tsumugi];
    }

    vector <int> ball(n);

    //後ろから見ていく
    // N/(i+1) < x < N/iの範囲で分析していけばよい
    for (int matsuri = 1; matsuri < n; matsuri++){
        
    }


}