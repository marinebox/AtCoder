// Integer Cards
#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    vector <long long> a(n);
    for (int tsumugi = 0; tsumugi < n; tsumugi++){
        cin >> a[tsumugi];
    }
    vector <long long> b(m), c(m);
    for (int matsuri = 0; matsuri < m; matsuri++){
        cin >> b[matsuri] >> c[matsuri];
    }

    
}